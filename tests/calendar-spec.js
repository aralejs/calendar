define(function(require) {
    var Calendar = require('../src/calendar');

    describe('Calendar', function() {
        test('initialize Calendar', function() {
            var cal = new Calendar();
        });
    });
});
